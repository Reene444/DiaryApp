<van-search value="{{ value }}" placeholder="请输入搜索关键词"/>
<block wx:for="{{admsList}}" wx:key="index">
  <van-cell title="{{item.name}}" label="{{item.phone}}" is-link link-type="navigateTo"
    url="../admin_info/index?adm_id={{item._id}}"/>
</block>